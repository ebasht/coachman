PORT=3001
# PostgreSQL:
# DATABASE_URL=postgres://user:pass@host:5432/coachman?sslmode=disable

# Required in production. For local/dev with empty/default secret set COACHMAN_DEV=1.
JWT_SECRET=change-me-to-a-long-random-value
# COACHMAN_DEV=1

CORS_ORIGIN=http://localhost:5173,http://localhost:3001
BOOTSTRAP_TOKEN=change-me-bootstrap-token
# After first admin exists, rebind/wipe stay off unless explicitly enabled:
# BOOTSTRAP_ALLOW_REBIND=1
# BOOTSTRAP_ALLOW_RESET=1
INVITE_TTL_HOURS=168

# Web Push (PWA уведомления в фоне). Сгенерировать: npm run generate:vapid
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# Email без mailto: (библиотека сама добавит) ИЛИ HTTPS URL сайта:
# VAPID_SUBJECT=https://your-host/
# Должен совпадать с id в PWA-манифесте (VITE_PWA_ID при сборке клиента):
# PWA_MANIFEST_ID=https://your-host/

# FCM (входящие звонки в Android APK). Без этого Web Push в WebView не разбудит приложение.
# FCM_PROJECT_ID=your-firebase-project-id
#
# Service account с private_key — ТОЛЬКО на сервере, не в git и не в APK.
# Вариант A — путь к файлу:
# FCM_SERVICE_ACCOUNT_JSON=/var/secrets/firebase-adminsdk.json
#
# Вариант B — сам JSON в .env (одна строка! jq -c . firebase-adminsdk.json):
# FCM_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",...}
#
# Не путать с client/android/app/google-services.json — тот только для Android-клиента.

# WebRTC TURN (видеозвонки через NAT). Без TURN работают только в одной сети / с простым NAT.
# Credentials are served only to authenticated clients via GET /api/ice-servers.
# TURN_URLS=turn:turn.example.com:3478,turns:turn.example.com:5349
#
# Режим coturn use-auth-secret (рекомендуется): только shared secret — API сам выдаёт временный пароль.
# TURN_SECRET=your-static-auth-secret
# Опционально id в username (формат expiry:id), по умолчанию coachman:
# TURN_USERNAME=coachman
# TTL временных creds в секундах (по умолчанию 86400):
# TURN_TTL_SECONDS=86400
#
# Либо статический логин/пароль (long-term credentials, без secret):
# TURN_USERNAME=coachman
# TURN_CREDENTIAL=secret
#
# Опционально свой список STUN (по умолчанию Google):
# STUN_URLS=stun:stun.l.google.com:19302
# Или полный JSON ICE (перекрывает STUN/TURN_*):
# ICE_SERVERS_JSON=[{"urls":"turn:turn.example.com:3478","username":"u","credential":"p"}]

# Опционально: Redis для fan-out WebSocket между инстансами
# REDIS_URL=redis://localhost:6379

# Fallback на SQLite (если DATABASE_URL не задан):
# DB_PATH=data/coachman.db

# Object storage (опционально; без S3_ENDPOINT — BLOB в PostgreSQL)
# With S3 enabled, chat photos upload directly browser → bucket (presigned PUT) and
# download directly browser → bucket (presigned GET) — never through nginx or Go.
# See docs/photo-uploads.md for bucket/CORS/CDN/IAM setup.
# Bucket CORS must allow your site origin for PUT/GET/HEAD — Yandex rejects "*" and
# the IAM key usually cannot set CORS, so configure it in the Yandex console manually.
# MinIO локально:
# S3_ENDPOINT=localhost:9000
# S3_ACCESS_KEY=minioadmin
# S3_SECRET_KEY=minioadmin
# S3_BUCKET=coachman
# S3_PUBLIC_URL=http://localhost:9000/coachman
#
# Yandex Object Storage (S3_* и YANDEX_STORAGE_* — взаимозаменяемые имена):
# S3_ENDPOINT=storage.yandexcloud.net           # или YANDEX_STORAGE_ENDPOINT
# S3_REGION=ru-central1                          # или YANDEX_STORAGE_REGION
# S3_ACCESS_KEY_ID=...                           # или YANDEX_STORAGE_ACCESS_KEY
# S3_SECRET_ACCESS_KEY=...                        # или YANDEX_STORAGE_SECRET_KEY
# S3_BUCKET=coachman                             # или YANDEX_STORAGE_BUCKET
# S3_USE_SSL=1
#
# Photo direct-upload settings (backend is the source of truth for limits):
# Приватная модель (по умолчанию): не задавайте CDN base — сервер выдаёт короткоживущие
# presigned GET URL. Публичная модель CDN: задайте YANDEX_CDN_BASE_URL и сделайте объекты
# читаемыми через CDN (URL с криптослучайным ключом — см. docs/photo-uploads.md).
# YANDEX_CDN_BASE_URL=https://cdn.example.com
# PHOTO_MAX_FILE_SIZE=31457280   # 30 MB (bytes), hard server-side limit
# PHOTO_UPLOAD_URL_TTL=600       # presigned PUT lifetime, seconds
# PHOTO_DOWNLOAD_URL_TTL=600     # presigned GET lifetime, seconds
